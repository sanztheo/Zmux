interface:
  display_name: "zmux Debug Windows"
  short_description: "Tune zmux debug windows and copy snapshots."
  default_prompt: "Use this skill to manage zmux sidebar/background/menu-bar debug windows, keep Debug menu wiring clean, and capture copyable debug snapshots."
