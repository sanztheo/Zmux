interface:
  display_name: "zmux Browser"
  short_description: "Automate zmux webview surfaces with snapshot/ref workflows."
  default_prompt: "Use this skill for browser automation via zmux CLI: identify target surface, snapshot interactive refs, perform actions, wait for state changes, and re-snapshot to avoid stale refs."
