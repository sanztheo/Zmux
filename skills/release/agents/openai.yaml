interface:
  display_name: "zmux Release"
  short_description: "Prepare and publish zmux releases from version bump to tag."
  default_prompt: "Use this skill to run the zmux release workflow: choose version, generate user-facing changelog updates, bump versions, open and monitor release PRs, merge, tag, and verify release artifacts."
