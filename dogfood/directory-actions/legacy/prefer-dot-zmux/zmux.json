{
  "actions": {
    "zmux.newTerminal": {
      "type": "command",
      "title": "Losing zmux.json",
      "subtitle": "You should not see this in prefer-dot-zmux",
      "command": "echo 'LOSING ZMUX.JSON'; echo 'THIS SHOULD NOT WIN'; pwd",
      "target": "newTabInCurrentPane",
      "icon": { "type": "emoji", "value": "❌" }
    },
    "legacy.loser": {
      "type": "command",
      "title": "Losing Action",
      "command": "echo 'LOSING ACTION'; pwd",
      "target": "newTabInCurrentPane",
      "icon": { "type": "emoji", "value": "❌" }
    }
  },
  "ui": {
    "surfaceTabBar": {
      "buttons": [
        "zmux.newTerminal",
        "legacy.loser"
      ]
    }
  }
}
